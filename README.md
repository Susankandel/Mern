-> GUI (Graphical User Interface)
-> CLI (Command Line Interface)

GIT COMMAND

1. CLONE
   => git clone <Project url>

2. add.
   => git add .
   => git add <file name>

3. commit
   => git commit -m "<msg>"

4. push
   => git push origin <branch>
